# Copy this file to .env and replace the empty value locally.
# Generate a token with `claude setup-token` (requires a Claude Pro/Max/Team/
# Enterprise subscription). Do not also set ANTHROPIC_API_KEY; it takes
# precedence over this token and bills per-token instead of using the
# subscription.
CLAUDE_CODE_OAUTH_TOKEN=

